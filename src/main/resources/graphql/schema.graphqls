schema {
    query: Query
}

type Author {
    authorId: ID,
    name: String,
    getBooksByAuthor: [Book]
}

type Book {
    bookId: ID,
    title: String,
    authors: [Author]
}

type Query {
    getAllBooks: [Book],
    getAuthor(name:String): Author,
}