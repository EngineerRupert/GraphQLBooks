schema {
    query: Query
}

type Author {
    authorId: ID!,
    name: String,
}

type Book {
    bookId: ID!,
    title: String,
    authors: [Author]
}

type Query {
    getAllBooks: [Book],
    getBooksByAuthor(name:String): [Book]
}